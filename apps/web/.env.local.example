# Where the deployed Worker lives. Local dev: leave empty to use same-origin /api.
# In static-export prod, the frontend is on Pages and the Worker is a separate origin
# — set this to the Worker URL and re-build.
NEXT_PUBLIC_API_BASE=
